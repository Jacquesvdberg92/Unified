@model Unified.Models.EmailTemplates.Brand
@{
    ViewData["Title"] = "Edit Brand";
}

<div class="d-md-flex d-block align-items-center justify-content-between my-4 page-header-breadcrumb">
    <h1 class="page-title fw-semibold fs-18 mb-0">Edit Brand — @Model.Name</h1>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="EditBrand" method="post" enctype="multipart/form-data">
            <input asp-for="Id" type="hidden" />
            @await Html.PartialAsync("_BrandForm", Model)
            <button type="submit" class="btn btn-primary mt-3">Save</button>
            <a asp-action="BrandManager" class="btn btn-secondary mt-3">Cancel</a>
        </form>
    </div>
</div>
